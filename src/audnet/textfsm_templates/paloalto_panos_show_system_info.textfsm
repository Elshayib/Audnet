Value HOSTNAME (\S+)
Value MODEL (\S+)
Value SERIAL (\S+)
Value OS (\S+)
Value FAMILY (\S+)

Start
  ^hostname:\s+${HOSTNAME}
  ^family:\s+${FAMILY}
  ^model:\s+${MODEL}
  ^serial:\s+${SERIAL}
  ^sw-version:\s+${OS} -> Record
