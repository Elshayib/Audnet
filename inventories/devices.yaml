defaults:
  device_type: cisco_ios
  port: 22
  timeout: 30

devices:
  - name: core-rtr-01
    host: 192.168.1.1
    username: admin
    password: "${NET_AUDIT_PASSWORD}"

  - name: dist-sw-01
    host: 192.168.1.2
    username: admin
    password: "${NET_AUDIT_PASSWORD}"
