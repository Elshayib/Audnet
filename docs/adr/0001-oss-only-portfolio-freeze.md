# This repo is OSS-only and frozen as a portfolio artifact

Audnet on GitHub is the MIT CLI: Operators collect live Device configuration and check it against a security baseline. We will not merge licensing, tenants, an MSP console, or a desktop shell. That work lived on `feat/enterprise-tenants` and is discarded. The author is not operating Audnet in production; the repository is a proof of skill, not a product being dogfooded.
